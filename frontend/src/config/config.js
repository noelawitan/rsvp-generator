export const BASE_URL = 'http://localhost:8081';
export const API_URL = `${BASE_URL}/api`;
export const LOGIN_URL = `${API_URL}/auth/authenticate`;
export const EVENT_URL = `${API_URL}/events`;
export const GUEST_URL = `${API_URL}/guests`;
export const INVITATION_URL = `${API_URL}/invitation`;